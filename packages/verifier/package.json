{
  "name": "@memora-hq/memora-verifier",
  "version": "0.1.0",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@memora-hq/memora-protocol": "0.1.0-rc.2",
    "ethers": "^6.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=18"
  },
  "description": "Offline verification of Memora local evidence: .memora bundles and on-disk session stores. No network calls, no native/OS-specific dependencies.",
  "license": "Apache-2.0",
  "sideEffects": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/memora-hq/memora-sdk.git",
    "directory": "packages/verifier"
  },
  "publishConfig": {
    "access": "public"
  }
}
